spring:
  datasource:
    url: jdbc:h2:mem:un_sec
    username: admin
    password: admin
  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

server:
  port: 9091
  servlet:
    context-path: /api

#logging:
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#    file: "%d{yyyy-MM-dd HH:mm:ss}[%thread]%-5level %logger{36} - %msg%n"

feign:
  protocol: http